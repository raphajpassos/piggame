'use strict';

//Selecting elements by ID we can use .GetElementById
const score0 = document.getElementById('score--0');
const score1 = document.getElementById('score--1');

score0.textContent = 0;
score1.textContent = 0;
